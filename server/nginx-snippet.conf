# Add inside the existing TLS (or Cloudflare-fronted) server block:
location /lead-api/ {
    proxy_pass http://127.0.0.1:8787/;
    proxy_set_header X-Forwarded-For $remote_addr;
    client_max_body_size 2m;
}
